import React from 'react';

const Form = () => (
  <div className="Form">
    <input placeholder="Next on the list..."></input>
    <button type="submit">Submit</button>
  </div>
)

export default Form;